export const headerStyles = {
  // Главный контейнер шапки
  container: [
    'fixed',      // Фиксированное позиционирование (шапка всегда видна)
    'top-0',      // Привязка к верхнему краю
    'left-0',     // Привязка к левому краю
    'right-0',    // Привязка к правому краю
    'z-50',       // Высокий z-index чтобы шапка была поверх других элементов
    'transition-all duration-300 ease-in-out',  // Плавная анимация для всех свойств
    'bg-background'  // Цвет фона из темы
  ].join(' '),

  // Состояния шапки
  states: {
    scrolled: 'shadow-sm',  // Тень при прокрутке
    hidden: '-translate-y-full',  // Скрытие шапки
    menuOpen: 'bg-background'  // Цвет фона при открытом меню
  },

  // Внутренний контейнер для содержимого шапки
  inner: [
    'container',   // Ограничение максимальной ширины контента
    'mx-auto',     // Центрирование по горизонтали
    'px-4',        // Отступы по бокам
    'flex',        // Флекс-контейнер
    'flex-wrap',   // Разрешить перенос элементов
    'items-center',  // Выравнивание элементов по центру по вертикали
    'justify-between'  // Распределение пространства между элементами
  ].join(' '),

  // Секция с логотипом
  logo: {
    wrapper: [
      'flex',        // Флекс-контейнер
      'items-center',  // Выравнивание по центру по вертикали
      'space-x-4'     // Отступ между элементами внутри
    ].join(' '),
    link: [
      'flex',        // Флекс-контейнер
      'items-center',  // Выравнивание по центру по вертикали
      'transition-opacity',  // Плавное изменение прозрачности
      'duration-300',  // Длительность анимации
      'hover:opacity-80'  // Прозрачность при наведении
    ].join(' '),
    icon: [
      'w-[94px]',        // Ширина иконки
      'h-[94px]',        // Высота иконки
      'text-secondary'  // Цвет иконки из темы
    ].join(' ')
  },

  // Навигационное меню
  nav: {
    container: [
      'w-full',      // Полная ширина на мобильных
      'md:w-auto'    // Автоматическая ширина на десктопе
    ].join(' '),
    list: [
      'flex',        // Флекс-контейнер
      'flex-col',    // Вертикальное расположение на мобильных
      'lg:flex-row',  // Горизонтальное расположение на десктопе
      'lg:space-x-[220px]'  // Увеличенные отступы между пунктами меню на десктопе
    ].join(' '),
    item: [
      'block',       // Блочный элемент
      'py-2',        // Вертикальные отступы
      'md:py-0',     // Убираем отступы на десктопе
      'text-[32px]',     // Увеличенный размер текста
      'font-medium',  // Жирность шрифта
      'text-primary',  // Основной цвет текста
      'transition-colors',  // Плавное изменение цвета
      'duration-300',  // Длительность анимации
      'hover:text-secondary'  // Цвет при наведении
    ].join(' ')
  }
};
